# Known Issues

- Snack budget rules are split between request data, employee start-date data, and approval code.
- The dashboard still shows old approved and rejected requests in the default view.
- The local data file is a demo feed. It is not connected to the old Microsoft Access snack database.
- The word "almonds" appears in several places without a linked incident report.
- Some over-budget approval cases do not surface the budget variance path consistently.
- The care team keeps requesting warmer snack language, but warmer language does not change the approval state.
- Requester intake should not disclose internal eligibility thresholds through visible copy or form metadata.
