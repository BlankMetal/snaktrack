# Architecture Proposal

This document describes a React-based replacement for the existing snack approval dashboard.

The proposal assumed a GraphQL snack gateway, a shared design system, and a finance workflow API. None of those are present in this repository.

Current recommendation from the last review: keep the existing browser app running until the next migration is funded.

